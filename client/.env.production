# This file is used when building in production.
# If you want to explicitly set the backend URL, define it here.
# Otherwise, leave it empty and the frontend will use the relative /api endpoint.

# VITE_API_URL=https://your-backend-host.com
